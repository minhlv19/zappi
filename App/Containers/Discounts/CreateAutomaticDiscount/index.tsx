import React, { useState } from 'react';
import { View, Text, SafeAreaView } from 'react-native';

const CreateAutomaticDiscount = () => {
  return (
    <SafeAreaView>
      <Text>CreateAutomaticDiscount</Text>
    </SafeAreaView>
  );
};

export default CreateAutomaticDiscount;
